#include "ProgressBar.h"

ProgressBar::ProgressBar(int min, int max, sf::Vector2f position, sf::Vector2f size)
{
}

void ProgressBar::draw(sf::RenderTarget& target, sf::RenderStates states) const
{
}
